<%= form_for(comment.replies.build) do |f| %>
<label> write reply </label><br>
<%=hidden_field_tag  'comment_id', comment.id%>
<%= f.text_area :body, :rows => "6" %>
<p>
<%= f.submit "reply", :class => "btn btn-primary"%>
</p>
<% end %>